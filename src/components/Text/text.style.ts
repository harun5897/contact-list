import { css } from '@emotion/react'
import styled from '@emotion/styled'

const paragraphTextStyle = () => css`
    font-size: 14px;
`
export const TextParagraph = styled.p`${paragraphTextStyle}`
export const TextLabel = styled.label`${paragraphTextStyle}`